/**
 * A module to easily inject vendors dependencies as angular service
 *
 * @module app/vendors
 * @exports app/vendors
 * @version 0.0.1
 */

window.angular && (function (angular) {
    'use strict';

    angular
        .module('app.vendors', [])
})(window.angular);
